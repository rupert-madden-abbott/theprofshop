$('#<%= plural_name %>').html('<%%= escape_javascript(render('table')) %>');
document.title = "<%%= escape_javascript("#{params[:search].to_s.titleize} Pages by #{(params[:sort] || 'name').titleize} - Page #{params[:page] || 1}") %>";
