- title "<%= plural_name.titleize %>"

= form_tag <%= plural_name %>_path, :method => 'get', :id => 'searchable', :autocomplete => 'off' do
  %p
    = text_field_tag :search, params[:search] 
    = image_submit_tag "search.png", :name => nil, :title => "Search"
  #<%= plural_name %>.archivable
    = render 'table'
